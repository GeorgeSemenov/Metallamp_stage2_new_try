mixin checkbox-list__label(modifier)
  if modifier.class.includes("checkbox-list_rich")
    h5.checkbox-list__label&attributes(attributes)!= modifier.text
  else
    span.checkbox-list__label&attributes(attributes)!= modifier.text
