mixin toggler-with-label__toggler(modifier)
  .toggler-with-label__toggler&attributes(attributes)
    +toggler-with-label__input(modifier)
    +toggler-with-label__knob(modifier)
    +toggler-with-label__knob-line(modifier)